module View.Annotation exposing (view)

import Data.Annotation as Annotation exposing (Annotation)
import Svg exposing (Svg)


view : Annotation -> Svg msg
view annotation =
    Debug.todo "Annotation.view"
